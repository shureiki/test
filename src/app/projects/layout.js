export const viewport = {
	themeColor: '#00FF00',
};

export const metadata = {
	title: 'Mes projets',
	description: 'Test',
};

export default function Layout({ children }) {
	return (
		<>{children}</>
	);
}
